<h1>Listing genres</h1>

<table cellpadding="10">
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= genre.title %></td>
        <td><%= link_to 'Show', genre %></td>
        <td><%= link_to 'Edit', edit_genre_path(genre) %></td>
        <td><%= link_to 'Destroy', genre, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h3>
    <%= link_to 'New Genre', new_genre_path %><br><br>
    <%= link_to "Home", root_path %>
</h3>
<div class="ftext"> Developed for VBPS Dwarka by Ujjwal "Ryzokuken" Sharma </div>
